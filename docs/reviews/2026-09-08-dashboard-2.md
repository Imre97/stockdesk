# Review: dashboard — 2026-09-08 (run 2, confirming)

Commit reviewed: `8cdd7be` (fix: address dashboard review blockers and should-fix items). Previous run: [2026-09-08-dashboard.md](2026-09-08-dashboard.md).

## Gate 1

- `npm run lint` → exit 0
- `npm test -- --run` → exit 0: api 202, web 190, shared 279
- `npm run typecheck` 0, `npm run build` 0, `npm run e2e` 19 passed

## Verdict

BLOCKED — 1 blocker, 1 should-fix, 1 nice-to-have: every run-1 finding (B1, B2, S1–S5 and the extras) is verified fixed and correctly tested, but reading the logout→login path while checking the `staleTime: Infinity` question surfaced that no client state is cleared on logout, so a second user in the same tab renders the previous user's accounts and balances. Gate 1 taken as reported; the cross-user finding was established by reading the code path, not by an executed test.

Claim verification (all confirmed): B1 `apps/web/src/features/dashboard/hooks.test.tsx:71,85,104,123` pins `getEquity("acc-1","1W")` after a range change and `getEquity("acc-2","1D")` after `setActiveAccount`, plus the empty flag. B2 `features/funding/store.ts` + `useSelectedFundingAccountId` drives both form and list; `hooks.test.tsx:135` proves `tx-new` lands on top of the Savings ledger and never enters the `acc-1` cache; e2e `deposit.spec.ts:57` keeps `aria-current="true"` on Main. S1 `NY_LOCAL_AT_SQL` anchors to UTC first, `accounts.equity.test.ts:115` fails under the old expression. S2 `accounts.daily-pnl.test.ts:45` pins EDT 13:30Z and strict `<`, `:63` EST 14:30Z, `:80` the Friday boundary, `:96` `"0.00"`, `:115` `referenceEquities` per account. S3 `user-registry.test.ts:26,42,59,73,95` + `auth-handshake.test.ts:110`. S4 `funding/hooks.ts:133` rejects `decimalPlaces() > 2` before `toApiString`, proven at `hooks.test.tsx:178`. S5 `accounts/form.ts`, `useProfileMenu`/`useSettingsForm` validate raw strings; no `.parse(`/`.mutate(` remains in any `.tsx` under `features/` or `routes/`. Regression checks clean: funding store cannot leak a foreign selection (membership check); `TZ: "UTC"` set in vitest and DST assertions are absolute instants; the equity `WHERE` still compares the bare `at` column so `@@unique([accountId, at])` remains the access path; all touched files are 11–190 lines.

## Acceptance criteria coverage

All 17 criteria covered; AC5 now also pins the 1Y New York-day bucket (`accounts.equity.test.ts:115`), AC10/AC11 the chart data half (`dashboard/hooks.test.tsx:85,104`), AC16 the non-active account ledger (`funding/hooks.test.tsx:135`, `deposit.spec.ts:57`).

## Findings

### Blockers

- `apps/web/src/features/shell/bootstrap.ts:17` — `if (settingsStore.status === "loaded" && accountsStore.status === "loaded") return;` plus a logout that only clears the auth store (`apps/web/src/features/auth/store.ts:65`) and navigates in-SPA: after user A logs out and user B logs in in the same tab, the bootstrap skips both fetches, so the sidebar renders A's account names, cash, equity and daily P&L, `activeAccountId` stays A's account id, and B's deposit or settings save targets it (`404 ACCOUNT_NOT_FOUND`) until the next `account_summary` tick or a page reload; `staleTime: Infinity` on the settings query makes the same staleness permanent on `/settings` within `gcTime`. Pre-existing since `8af9d47`, not a regression of `8cdd7be`. Reset the accounts, settings and funding stores and call `queryClient.clear()` in `logout`, or key the bootstrap guard by user id; test with a logout followed by a login as a second user asserting the sidebar shows only the second user's accounts.

### Should fix

- `apps/api/test/accounts.daily-pnl.test.ts:63` — the DST-end case uses `2026-11-02`, so the day the spec names (`2026-11-01`) is only covered for DST start; a `2026-11-01` case would exercise weekend fallback across the offset change (Friday 2026-10-30 09:30 EDT = 13:30Z). Add the case or reword the spec.

### Nice to have

- `apps/web/src/features/funding/store.ts:9` — `selectedAccountId` is never cleared; harmless only because `useSelectedFundingAccountId` re-checks membership. Reset it with the other stores when the logout reset lands.

## Optimization opportunities

- `apps/web/src/features/funding/hooks.ts:47` — `useAccountsStore((state) => state.accounts)` subscribes the deposit page to the whole array, replaced on every `account_summary` tick. Select the boolean instead. Later.

## Tech debt candidates

- No client state reset on logout (blocker above).
- Spec names DST days 2026-03-08 and 2026-11-01; tests cover 2026-03-08, 2026-03-09 and 2026-11-02.
- Deposit page re-renders on every `account_summary` tick through the `accounts` array selector.
