# Review: auth — 2026-09-08 (run 6, quick delta)

Commit reviewed: `cae972a` (fix: scan untracked files, prod proxy default, prune invariant test). Previous run: [2026-09-08-auth-5.md](2026-09-08-auth-5.md). Per `docs/05-lessons.md` L-11 this is the last round for Module 1: the module is `implemented`, no blocker was found, and the nice-to-have items are registered as tech debt (TD-18 to TD-20) instead of triggering another fix round.

## Gate 1

- `npm run lint` → exit 0
- `npm test -- --run` → exit 0: api 90, web 61, shared 107
- `npm run typecheck` 0, `npm run build` 0, `npm run e2e` 7 passed

## Verdict

PASS — 0 blockers, 0 should-fix, 4 nice-to-have; all four claimed fixes are present and test-backed, and the regression probes (scan scope, scan time, proxy type, doc/env consistency) came back clean. Gates were run by the caller and not re-run here; the reviewer re-ran only `node scripts/check-language.mjs` (exit 0, 0.12 s over 143 tracked + 0 untracked files).

## Coverage of the changes

| # | Change (short) | Test file:line | Status |
|---|---|---|---|
| 1 | Language guard scans untracked files, honours `.gitignore` | `apps/api/test/check-language.test.ts:54,63-65` | covered |
| 2 | Revoked-but-unexpired refresh row survives boot prune (L-9) | `apps/api/test/boot.test.ts:38,52` | covered |
| 3 | `TRUST_PROXY_HOPS` defaults 1 in production, 0 elsewhere, explicit value wins | `apps/api/src/lib/config.test.ts:79,85,90` | covered |
| 4 | Boot prune failure reports the cause | `apps/api/test/boot.test.ts:86,93` | covered |

Regression probes: `git ls-files --cached --others --exclude-standard` in the real repo returns 143 files and no untracked entries (`dist/`, `node_modules/`, `coverage/`, `.env*` all covered by `.gitignore`, so no build output enters the scan and the full run stays at ~0.12 s); `config.ts:34,66` types `trustProxyHops` as `number` and the `??` fallback keeps it a number, so `app.set("trust proxy", config.trustProxyHops)` at `apps/api/src/app.ts:56` still receives a number, never `true`; the temp-git test creates no commits, so it needs no `user.name`/`user.email`, and `afterEach` `rmSync` removes every `mkdtempSync` root; `.env.example`, `render.yaml` (`TRUST_PROXY_HOPS: "1"`), `docs/modules/auth.md` and `docs/04-deployment.md` agree on the new default; no secret value entered the diff.

## Findings

### Blockers

- None.

### Should fix

- None.

### Nice to have

- `apps/api/src/server.ts:16` — the new `.catch` logs `Starting the boot tasks failed: …` and lets the process keep serving with no prune timer ever installed, so token-row growth resumes silently until the next deploy. Decide explicitly: exit non-zero, or retry the scheduling. → TD-18
- `scripts/check-language.mjs:67` — repo mode is selected by `root === repoRoot || process.env.CHECK_LANGUAGE_GIT === "1"`, a flag that exists only for the test, so the real selection predicate is never exercised. Detect `.git` under `root` instead and drop the env hook. → TD-19
- `apps/api/test/check-language.test.ts:56` — `spawnSync("git", ["init", "--quiet"], …)` ignores its status; if git is absent the script throws inside `repositoryFiles` and the failure surfaces as a missing-substring assertion. Assert `status === 0` first. → TD-20
- `docs/04-deployment.md:56` — the env block omitted `AUTH_RATE_LIMIT_MAX` and `AUTH_RATE_LIMIT_WINDOW_MINUTES`, which are in `.env.example` and in `config.ts`. → fixed inline in the same commit as this report (documentation only).

## Optimization opportunities

- `scripts/check-language.mjs:111` — full-tree scan per `npm run lint` measured at 0.12 s today; no action needed until fixtures grow (later).

## Tech debt candidates

- Boot-task scheduling failure leaves refresh-token pruning permanently off with only a stderr line (TD-18).
- Language guard's repo-mode selection depends on a test-only `CHECK_LANGUAGE_GIT` env flag instead of detecting a git root (TD-19).
- `check-language.test.ts` ignores the `git init` exit status (TD-20).
