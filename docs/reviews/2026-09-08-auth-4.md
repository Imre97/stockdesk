# Review: auth — 2026-09-08 (run 4, quick delta)

Commit reviewed: `a86a118` (fix: keep rotated refresh tokens as reuse tombstones on logout). Previous runs: [2026-09-08-auth.md](2026-09-08-auth.md), [2026-09-08-auth-2.md](2026-09-08-auth-2.md), [2026-09-08-auth-3.md](2026-09-08-auth-3.md).

## Gate 1

- `npm run lint` → exit 0
- `npm test -- --run` → exit 0: api 14 files / 69 tests, web 7 / 61, shared 3 / 121
- `npm run typecheck` 0, `npm run build` 0, `npm run e2e` 7 passed

## Verdict

PASS — all four claims verified in `a86a118`, no regressions found: 0 blockers, 0 should-fix, 1 nice-to-have.

## Acceptance criteria coverage

Delta scope only (AC 1–4, 7–9 verified in runs 2 and 3, untouched by this commit).

| # | Criterion (short) | Test file:line | Status |
|---|---|---|---|
| 5 | Refresh rotates; reuse → `401 REFRESH_REUSED`, all sessions revoked | `apps/api/test/auth.logout.test.ts:64` (reuse survives logout), plus pre-existing `apps/api/test/auth.refresh.test.ts` | covered |
| 6 | Logout clears cookie; following refresh → `401` | `apps/api/test/auth.logout.test.ts:9,29,64,87` (5 tests, 4 pre-existing unchanged) | covered |
| — | Error codes constrained to shared union | `apps/api/src/middleware/error-handler.test.ts:50` (`@ts-expect-error`), `:70` and `apps/api/test/not-found.test.ts:14` (membership) | covered |

Verified claims: (1) `apps/api/src/modules/auth/repository.ts:84` is `deleteMany({ where: { tokenHash, revokedAt: null } })`; new tombstone test asserts `204` → `REFRESH_REUSED` → successor `401` → 0 live rows for the user. (2) `packages/shared/src/api-error.ts:18` exports `ErrorCode = AuthErrorCode | ApiErrorCode`; `AppError.code` and `ErrorEnvelope.code`/`envelope()` typed with it; all 8 `new AppError(...)` producers compile under the union; stale `400 BAD_REQUEST` fixture replaced by `422 VALIDATION_ERROR`. (3) tautological `API_ERROR_CODES` block deleted from `packages/shared/src/api-error.test.ts`. (4) `docs/modules/auth.md:50` documents live-row-only deletion and tombstone retention. Targeted re-run: 10/10 passed.

Regression checks requested:

- `revokedAt: null` vs expired-but-live rows: no interaction — an expired live row is still deleted by logout, and `rotateRefreshToken` (`repository.ts:99`) rejects any expired presented token as `invalid` before the revocation branch, so expiry ordering is unchanged. Retained tombstones are bounded: `deleteExpiredRefreshTokens` (`repository.ts:88`) filters on `expiresAt` only, so revoked rows are reaped at TTL.
- Import cycle: none. `packages/shared/src/auth.ts` imports only `zod`; the `AuthErrorCode` edge into `api-error.ts` is one-directional and type-only.
- `@ts-expect-error` vs `docs/02-conventions.md:77`: it is a compiler directive, not explanatory prose, it carries the required description, and `apps/api/tsconfig.json` includes `test`, so `npm run typecheck` fails if the directive stops being needed. Consistent.

## Findings

### Blockers

- None.

### Should fix

- None.

### Nice to have

- `apps/api/src/middleware/error-handler.test.ts:53` — `expect(invalid.code).toBe("NOT_A_CODE")` asserts a value the type system forbids; the compile-time guard is the whole point, the runtime line only keeps the binding used. Replace with a `void invalid;`-free formulation such as asserting the constructed instance is an `AppError`, or drop the runtime expectation and keep the directive.

## Optimization opportunities

- None new in this delta. `deleteRefreshToken` remains a single indexed `deleteMany` on `tokenHash`.

## Tech debt candidates

- Logout presented with an already-rotated cookie returns `204` while the live successor session survives; documented behavior, but a client with a stale cookie gets a silent no-op logout.
- Revoked tombstones now live until `expiresAt`, so `RefreshToken` row count per user grows with rotation frequency inside one refresh TTL window; revisit if rotation interval shrinks.
