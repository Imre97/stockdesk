# Technical Debt Register

Last updated: 2026-09-08

## Summary
- Open: 32 (high 1, medium 9, low 22)
- Fixed since last update: 0
- Trend: the confirming review verified all four original blockers and six should-fix items fixed, but found one new blocker of the same class as TD-38 (a coverage/store mismatch, this time in the candle cache marking a still-forming bucket as covered without storing it); a narrow third round is fixing it along with the two should-fix items found alongside it.
- Recommended next: TD-28 (snapshot-writer redundant queries and unpaged `listAllAccounts`, still scheduled at this module), TD-56 (batch per-position price lookups before Module 4 fills positions), TD-53 (symbol search has no index and no rate limit before the real Alpaca universe lands).

## Open
| Id | Area | Summary | Impact | Effort | Timing | Source | Status |
|----|------|---------|--------|--------|--------|--------|--------|
| TD-4 | infra | Production static-serving path (helmet CSP + `express.static` SPA fallback + one-origin cookie) is only covered by a non-helmet unit test, never exercised end to end | medium | M | later | docs/reviews/2026-09-08-auth.md | open |
| TD-16 | api | Logout presented with an already-rotated cookie returns `204` while the successor session survives; documented no-op, but a client with a stale cookie gets a silent logout that doesn't logout | low | S | later | docs/reviews/2026-09-08-auth-4.md | open |
| TD-17 | api | Revoked refresh-token tombstones live until `expiresAt`, so per-user row count grows with rotation frequency inside one TTL window | low | S | later | docs/reviews/2026-09-08-auth-4.md | open |
| TD-18 | api | Boot-task scheduling failure (`server.ts` `.catch`) logs to stderr and keeps serving with no prune timer installed, so token-row growth resumes silently until the next deploy; decide between exit non-zero and retry | low | S | later | docs/reviews/2026-09-08-auth-6.md | open |
| TD-19 | infra | Language guard repo mode is selected by `root === repoRoot` or the test-only `CHECK_LANGUAGE_GIT` flag; detect a `.git` root instead and drop the env hook | low | S | later | docs/reviews/2026-09-08-auth-6.md | open |
| TD-20 | infra | `check-language.test.ts` ignores the `git init` exit status; an absent git surfaces as a missing-substring assertion instead of a clear failure | low | S | later | docs/reviews/2026-09-08-auth-6.md | open |
| TD-21 | web/test | Radix dropdown-menu content in jsdom takes 2 to 20 s per open render and triggers vitest RPC timeouts, so `ProfileMenu` open-state is not unit-tested (menu logic covered via `useProfileMenu` hook test, open menu via e2e) | low | S | later | docs/modules/dashboard.md implementation, 2026-09-08 | open |
| TD-23 | e2e/infra | On Windows a Playwright webServer `tsx watch src/server.ts` process survived the run once (orphan on port 3000 blocked the next run; killed manually); consider `tsx src/server.ts` without watch or an explicit teardown that kills the port; did not reproduce across three e2e runs on 2026-09-08, left open pending more evidence | low | S | later | docs/modules/dashboard.md implementation, 2026-09-08 | open |
| TD-25 | api/web | Dead code: `wsAuthenticatedUserId` (`apps/api/src/ws/auth-handshake.ts:23`) still has no consumer as of the market-data module, `depositViaApi` e2e helper (`apps/e2e/tests/helpers.ts:119`) is unused, `ProfileMenu`'s `defaultOpen` prop (`apps/web/src/features/shell/components/ProfileMenu.tsx:20`) exists only for tests, and the `Array.isArray(request.params.id)` branch in `apps/api/src/modules/accounts/router.ts:28` is unreachable for Express route params | low | S | later | docs/reviews/2026-09-08-dashboard.md | open |
| TD-27 | api | `apps/api/src/modules/accounts/deposits.ts:55` recomputes account summaries via `summarizeAccounts` after `afterCashChange` already built them in the snapshot writer, one redundant `referenceEquities` round trip per deposit | low | S | now | docs/reviews/2026-09-08-dashboard.md | open |
| TD-28 | api | `apps/api/src/modules/accounts/snapshot-writer.ts` issues one `referenceEquities` query per user per tick and broadcasts to users with no open sockets; `listAllAccounts()` (`repository.ts:66`) loads every account of every user each tick with no paging | medium | M | module: market-data | docs/reviews/2026-09-08-dashboard.md | open |
| TD-29 | api | `CashTransaction.@@index([accountId, createdAt])` (`schema.prisma:69`) omits `id`, while the ledger keyset page orders by `(createdAt desc, id desc)` | low | S | module: portfolio | docs/reviews/2026-09-08-dashboard.md | open |
| TD-46 | api | `bar-writer.ts:39` caches a `null` symbol id forever, so a symbol first seen before the boot refresh created its row never gets live bars persisted until the process restarts | low | S | later | docs/reviews/2026-09-08-market-data.md, docs/reviews/2026-09-08-market-data-2.md | open |
| TD-47 | api | `price-service.ts:102` `lastTrades` and `ws/quote-feed.ts:41` `prevCloses` are never pruned on `releaseStreaming`, and `composite.ts` `claimedByReal` is never released either, so these maps keep serving/blocking state for streams and symbols nobody listens to any more | medium | S | later | docs/reviews/2026-09-08-market-data.md, docs/reviews/2026-09-08-market-data-2.md | open |
| TD-48 | web | `PriceChart.tsx:50` wires `MutationObserver` and `ResizeObserver` imperatively inside a `.tsx`, breaking the render-only rule; move it into a `use-chart-instance.ts` hook | low | S | later | docs/reviews/2026-09-08-market-data.md | open |
| TD-49 | api | `ws-quotes.test.ts:154` polls with a real `setTimeout` for up to 2 s instead of a fake clock or an event hook | low | S | later | docs/reviews/2026-09-08-market-data.md | open |
| TD-50 | api | `simulated/provider.ts:65` builds trade size with `1 + Math.floor(stream.next() * MAX_TRADE_SIZE)`, native arithmetic on a quantity before it becomes a `Decimal` | low | S | later | docs/reviews/2026-09-08-market-data.md | open |
| TD-51 | api | `jobs.ts:70` `schedule` uses the global `setInterval` instead of injectable timers, so the recurring thinning/refresh schedule has no fake-clock test | low | S | later | docs/reviews/2026-09-08-market-data.md | open |
| TD-53 | api | `symbols-repository.ts:203` `"name" ILIKE '%q%'` cannot use an index; with the real Alpaca universe (~11k rows) every search keystroke is a sequential scan, and `/market/symbols/search` has no rate limit either | medium | M | later | docs/reviews/2026-09-08-market-data.md | open |
| TD-54 | api | `symbols-repository.ts:146` sends the entire asset list as `notIn` bind parameters in one statement instead of a temporary table or a `NOT EXISTS` join | low | M | later | docs/reviews/2026-09-08-market-data.md | open |
| TD-55 | api | `candles.ts:43` (`toApiString` at `:46`) reparses each `Prisma.Decimal` through a string into a new `Decimal` for five fields per bar, ~1500 allocations per 300-bar page | low | S | later | docs/reviews/2026-09-08-market-data.md, docs/reviews/2026-09-08-market-data-2.md | open |
| TD-56 | api | `accounts/summary.ts:56` `valuePositions` awaits `getLastPrice` serially per position, each miss doing a symbol lookup plus a candle read; needs batching before Module 4 fills positions | medium | M | module: orders | docs/reviews/2026-09-08-market-data.md | open |
| TD-58 | api | `apps/api/src/modules/market/providers/composite.test.ts` is 346 raw lines, over the 300-line soft limit though under the 400 hard limit | low | S | later | docs/reviews/2026-09-08-market-data.md fix round | open |
| TD-59 | api | `candles.ts:120` `addCoverage(..., from, range.end)` marks the whole fetched window covered although `:99` dropped the bucket still forming at `range.end`, so that bucket is never fetched again after it closes; a `1D` series requested once per day intraday freezes on stale bars | high | M | now | docs/reviews/2026-09-08-market-data-2.md | open |
| TD-60 | api | `bar-aggregator.ts:226` discards the unsubscribe returned by `priceService.onTrade`; `stop()` (`:265`) neither detaches the handler nor clears `forming`, so a trade arriving after `await writer.flush()` enqueues a write nobody awaits; `runtime.ts:103` also flushes the aggregator before `priceService.stop()` at `:104` | medium | S | now | docs/reviews/2026-09-08-market-data-2.md | open |
| TD-61 | api | `candles.ts:135` `windowStartMs(until, "1M", 300)` with the default limit steps 300 calendar months back, so a bare `?timeframe=1M` request asks the provider for a 25-year range and covers it in one coverage row | medium | S | now | docs/reviews/2026-09-08-market-data-2.md | open |
| TD-62 | web | `bars-consumers.ts:5` `counts` is module state outside the zustand store and is not cleared by `resetClientState`, so a reset that does not unmount the consumers leaves a series pinned for the next session | low | S | later | docs/reviews/2026-09-08-market-data-2.md | open |
| TD-63 | api | `ws/market-channels.ts:87` one 100-symbol subscribe of unknown symbols produces 100 `SYMBOL_NOT_FOUND` frames; the raised `QUOTE_MESSAGE_SYMBOL_CAP` doubled the amplification factor with no per-socket message budget | low | S | later | docs/reviews/2026-09-08-market-data-2.md | open |
| TD-64 | api | `ws-quotes.test.ts:227` the subscription-limit test settles with real-time waits (`SETTLE_MS = 300`, `TICK_WINDOW_MS = 400`) instead of a fake clock or an event hook | low | S | later | docs/reviews/2026-09-08-market-data-2.md | open |
| TD-65 | api | `bar-aggregator.test.ts:305` `await new Promise((resolve) => setTimeout(resolve, 0))` measures "not settled yet" with a real macrotask instead of a deterministic `vi.waitFor` | low | S | later | docs/reviews/2026-09-08-market-data-2.md | open |
| TD-66 | infra | `apps/api/src/server.ts:60` has no `SIGTERM` handler, so `market.stop()` never runs in production and the flush added to `stop()` only protects the tests | medium | S | later | docs/reviews/2026-09-08-market-data-2.md | open |
| TD-67 | api | `ws/market-channels.ts:105` sends subscribe-time snapshots with one `await` per added symbol, up to 50 per message; cheap today but becomes a serial chain of `getPrevClose` reads once positions exist | medium | S | module: orders | docs/reviews/2026-09-08-market-data-2.md | open |

## Closed
| Id | Area | Summary | Fixed in | Closed on |
|----|------|---------|----------|-----------|
| TD-10 | api | `trust proxy` hop count was hard-coded instead of read from config | `249ab11` (`config.ts` `TRUST_PROXY_HOPS`, `apps/api/test/auth.rate-limit.test.ts`) | 2026-09-08 |
| TD-11 | api | Three `as RotationResult` casts in `repository.ts` defeated return-type checking inside the rotation transaction | `249ab11` (`repository.ts:99` typed `$transaction` callback return) | 2026-09-08 |
| TD-12 | api | Boot-time refresh-token pruning was untestable because `server.ts` starts listening on import | `249ab11` (`apps/api/src/boot.ts` `runBootTasks`, `apps/api/test/boot.test.ts`) | 2026-09-08 |
| TD-13 | api | `eslint.config.js` test override also disabled `no-restricted-syntax`, letting test files do `parseFloat`/`toFixed` money math | `249ab11` (`eslint.config.js` `testGlobs` includes `apps/api/test/**` under the decimal rule) | 2026-09-08 |
| TD-14 | api | Every successful refresh rotation awaited an extra `DELETE` round trip in the request path | `249ab11` (`boot.ts` interval job; rotation path in `repository.ts` no longer prunes) | 2026-09-08 |
| TD-15 | api | The refresh-reuse race test repeated 5 iterations at bcrypt cost 12 | `249ab11` (`apps/api/test/auth.refresh.test.ts:58` loops 2 iterations) | 2026-09-08 |
| TD-7 | api | `RefreshToken` rows were never pruned; every login/refresh inserted a row and only flipped `revokedAt`, unbounded growth on Neon free | `6bfe977` (`repository.ts:91-95,126`, `server.ts:22`, test `auth.refresh.test.ts:78`) | 2026-09-08 |
| TD-8 | api | `RefreshToken` had no index on `userId`, but reuse detection and cascade delete both filter by it | `6bfe977` (migration `20260908084827_refresh_token_user_index`) | 2026-09-08 |
| TD-30 | infra | Stop hook and a subagent's vitest run hit the shared `stockdesk_test` database concurrently | `0225027` (`scripts/hooks/test-lock.mjs`, `apps/api/test/global-setup.ts` writes `.vitest-api.lock`, `run-tests.mjs` skips the api workspace while the lock is held by a live pid) | 2026-09-08 |
| TD-31 | web | `resetClientState` persisted in-memory defaults over the cached language and theme when the settings store was never hydrated | `0225027` (`apps/web/src/features/settings/store.ts` `reset()` patches only `defaultAccountId` via `patchCachedSettings`) | 2026-09-08 |
| TD-32 | web | `account_summary` socket closed only by layout unmount, so a late message could repopulate the outgoing user's accounts | `0225027` (`apps/web/src/lib/ws-session.ts` singleton, `resetClientState` calls `wsSession.disconnect()` first, cross-seam test in `session-reset.test.ts`) | 2026-09-08 |
| TD-33 | web | Duplicate `resetClientState` call sites in `useLogout` and `clearSession` | `0225027` (`useLogout` no longer resets; `clearSession` → `resetSessionState` is the single path) | 2026-09-08 |
| TD-24 | api | Trading day is approximated as Monday to Friday in New York with no holiday calendar until Module 3's market status (recorded decision, revisit in Module 3) | `9be834c` (`apps/api/src/modules/market/holidays.ts`, `apps/api/src/modules/market/calendar.ts` add the NYSE holiday calendar; `apps/api/src/modules/accounts/ny-time.ts` `isNyTradingDay` now excludes holidays too) | 2026-09-08 |
| TD-26 | api | `apps/api/src/ws/user-registry.ts` has no ping/pong heartbeat sweep, so half-open sockets stay registered until the process restarts | `b5b604d` (`apps/api/src/ws/heartbeat.ts` `startHeartbeat`, wired into `auth-handshake.ts`) | 2026-09-08 |
| TD-5 | web | `apps/web/src/i18n/index.ts` eager-loaded every locale catalog into the main chunk | `819b2a4` (`apps/web/src/i18n/index.ts` preloads only `PRELOADED_NAMESPACES`; `catalogs.ts` and `backend.ts` load every other namespace through `importCatalog` dynamic import) | 2026-09-08 |
| TD-34 | web | Query keys such as `["settings"]` were not user-scoped | `819b2a4` (`apps/web/src/lib/query-keys.ts` `userScopedKey` prefixes every cache key with the user id) | 2026-09-08 |
| TD-35 | e2e | No e2e coverage for a second user logging in within the same tab, or for preference survival after an expired session | `7ac1544` (`apps/e2e/tests/market.spec.ts:258` second-user-in-same-tab test, `:308` language-survives-session-expiry test) | 2026-09-08 |
| TD-22 | e2e | The Playwright API webServer booted before `globalSetup` ran `prisma migrate deploy`, so the boot snapshot and thinning jobs logged `table AccountEquitySnapshot does not exist` against a fresh stockdesk_e2e | `7ac1544` (`apps/e2e/prepare-database.ts` migrates and truncates before the webServer command starts the API; wired into `apps/e2e/playwright.config.ts` webServer `command`) | 2026-09-08 |
| TD-36 | web | AVGO-style dividend yield rendered 100x too small: `panel-mappers.ts:62` fed the API's fraction (e.g. `"0.0130"`) straight into `formatPercent`, which only appends a `%` sign, instead of multiplying by 100 | `14a19ae` (`apps/web/src/features/market/mappers.ts` adds `formatYield`, multiplying by `PERCENT_FACTOR` before formatting; `panel-mappers.ts` uses it for `stats.dividendYield`) | 2026-09-08 |
| TD-37 | web | `panel-mappers.test.ts:36` fixture used a non-fraction `dividendYield` value (`"1.25"`), so the `"1.25%"` assertion passed while the real API-to-display pipeline (fraction `"0.0130"` should render `"1.30%"`) was wrong | `14a19ae` (`panel-mappers.test.ts` fixture now `"0.0130"`, asserts `"1.30%"` and the Hungarian locale rendering) | 2026-09-08 |
| TD-38 | api | `candles.ts:100` decided bar completeness with the fixed `timeframeDurationMs` instead of the New York calendar stepper, permanently dropping finished calendar buckets and inserting still-forming ones as final | `14a19ae` (`apps/api/src/modules/market/candles.ts` now uses `nextBucketStartMs`/`windowStartMs` from `timeframes.ts`; `apps/api/test/market.bar-buckets.test.ts` covers the February `1M` bucket and the DST fall-back `1D` bucket) | 2026-09-08 |
| TD-39 | api | No test drove a socket past 50 quote subscriptions and asserted the wire-level `{"type":"error","code":"SUBSCRIPTION_LIMIT"}`; AC6 was proven only at the unit level | `14a19ae` (`apps/api/test/ws-quotes.test.ts` "subscribes the symbols that fit and refuses the overflow with one SUBSCRIPTION_LIMIT") | 2026-09-08 |
| TD-40 | shared | `packages/shared/src/ws.ts:17` capped the subscribe-message symbol array at `QUOTE_SUBSCRIPTION_LIMIT`, so a single over-limit subscribe failed schema validation and was silently ignored instead of admitting the ones that fit | `14a19ae` (`packages/shared/src/ws.ts` adds `QUOTE_MESSAGE_SYMBOL_CAP`, twice the subscription limit) | 2026-09-08 |
| TD-41 | api | `bar-writer.ts:59` `save` did not order writes per series, so a `finalizeBar` could apply before a `saveFormingBar` for the same `(symbolId, timeframe, time)`, leaving a closed bucket persisted with `isFinal = false` | `14a19ae` (`apps/api/src/modules/market/bar-writer.ts` chains writes per series key; `bar-writer.test.ts` proves issue order and that one slow series does not block another) | 2026-09-08 |
| TD-42 | api | `composite.test.ts:264` did not prove that `subscribeTrades` for a symbol already claimed by a real provider refuses to fall back to simulated | `14a19ae` (`apps/api/src/modules/market/providers/composite.test.ts` "never streams simulated ticks for a symbol a real provider already served") | 2026-09-08 |
| TD-43 | api | `market.bars.test.ts:34` and `ws-bars.test.ts:79` never validated the payload with `barsResponseDtoSchema` / `barMessageSchema` nor called `expectNoMonetaryNumbers` | `14a19ae` (both test files add a `validate()` helper calling `expectNoMonetaryNumbers` and parsing the DTO schema) | 2026-09-08 |
| TD-44 | web | `apps/web/src/features/market/store.ts:9` keyed `bars` by `symbol:timeframe` and only cleared them via `clearBars`/`reset`; `hooks.ts:129` never called `clearBars`, so every visited symbol and interval kept ~300 bar objects for the session | `14a19ae` (`apps/web/src/features/market/bars-consumers.ts` `acquireBars` reference-counts series and clears them when the last consumer releases; wired into `hooks.ts`) | 2026-09-08 |
| TD-45 | api | `symbols.ts:111` `storeProfile` stamped `profileFetchedAt` and `metricsFetchedAt` with the same instant and always refetched both, forcing a full profile refetch every hour instead of the spec's 24-hour profile / 1-hour metrics split | `14a19ae` (`apps/api/src/modules/market/symbols.ts` `getProfile(symbol, { parts })` and `upsertProfile`/`storeProfile` refresh profile and metrics independently; `apps/api/test/market.profile-refresh.test.ts`) | 2026-09-08 |
| TD-52 | infra | `schema.prisma:132` `@@index([symbolId, timeframe, time])` on `Candle` duplicated the index the `@@unique` on the same columns already creates | `14a19ae` (migration `apps/api/prisma/migrations/20260908180459_drop_redundant_candle_index` drops `Candle_symbolId_timeframe_time_idx`) | 2026-09-08 |
| TD-57 | api | `market-channels.ts:82` `knownSymbols` issued one `findActiveSymbol` round trip per symbol (up to 50 per subscribe message) instead of one `findMany` with `symbol: { in: [...] }` | `14a19ae` (`apps/api/src/modules/market/symbols-repository.ts` adds `findActiveSymbols`; `apps/api/src/ws/market-channels.ts` `knownSymbols` uses it) | 2026-09-08 |
